services:
  - type: web
    name: easyocr-app
    env: python
    buildCommand: |
      pip install -r requirements.txt
    startCommand: gunicorn app:app

    envVars:
      - key: python_version
        value: "3.10.13"